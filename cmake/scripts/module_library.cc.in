#include <string>

std::size_t ${module_lib_mangled}_version()
{
  return ${PROJECT_VERSION_MAJOR} * 10000 + ${PROJECT_VERSION_MINOR} * 100 + ${PROJECT_VERSION_PATCH};
}

std::string ${module_lib_mangled}_version_string()
{
  return "${PROJECT_VERSION}";
}
